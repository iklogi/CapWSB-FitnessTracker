CREATE TABLE users (
                       id INT PRIMARY KEY,
                       birthdate DATE,
                       email VARCHAR(255),
                       first_name VARCHAR(255),
                       last_name VARCHAR(255)
);
